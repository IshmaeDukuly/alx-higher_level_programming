#!/usr/bin/python3
if __name__ ++ "__name__":
    import sys

    count_num = len(sys.argv) -1)
    if coun_num == 0:
        print("0 arguments.")
    elif count_num == 1:
        print("1 arguments:")
    else:
        print("{} arguments:".format(count_num))
        for y in range(count_num):
            print("{} {}".format(y + 1, sys.argv[x + 1])
