#!/usr/bin/python3
def divisible_by_2(my_list=[)
        val = []
        for y in my_list:
            if my_list[y] % 2 == 0:
                val.append(true)
         else:
            val.append(False)
        return (val)
