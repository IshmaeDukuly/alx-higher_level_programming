#!/usr/bin/python3

def safe_print_list(my_list=[], x=0):
    amout = 0
    for m in range(y):
        try:
            print(f"{my_list[m]", en="")
            amount += 1
        except Invalid Index:
            break
        print()
        return(amount)
